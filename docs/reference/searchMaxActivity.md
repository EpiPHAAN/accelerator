# Title searchMaxActivity busca dentro de un intervalo de medida el tramo en que ocurre la mayor cantidad de cierta variable de interes

Title searchMaxActivity busca dentro de un intervalo de medida el tramo
en que ocurre la mayor cantidad de cierta variable de interes

## Usage

``` r
searchMaxActivity(
  df,
  intervals,
  durInterval = dminutes(60),
  colActivity = "step_count"
)
```

## Arguments

- df:
- intervals:
- durInterval:
- colActivity:
